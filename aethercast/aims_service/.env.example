# Service host and port
AIMS_HOST=0.0.0.0
AIMS_PORT=8000

# Flask debug mode
# Debug mode is typically controlled by FLASK_DEBUG in common.env or the execution environment.
# Example: FLASK_DEBUG=true
# FLASK_DEBUG=false

# --- Google Cloud Vertex AI Configuration ---
# Path to the GCP service account key JSON file *inside the Docker container*.
# Typically /app/gcp-credentials.json. Place the actual key file in aethercast/aims_service/ as gcp-credentials.json.
# See the main project README's 'GCP Prerequisites and Setup' section for details.
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-credentials.json

# Your Google Cloud Project ID.
# This is typically set in the common.env file at the project root.
# Override here only if a specific project is needed for this service. See main README's GCP setup guide.
GCP_PROJECT_ID=your-gcp-project-id

# The Google Cloud location/region for Vertex AI (e.g., us-central1).
# This is typically set in the common.env file at the project root.
# Override here only if a specific location is needed. See main README's GCP setup guide.
GCP_LOCATION=us-central1

# Default Google LLM Model ID for AIMS (e.g., gemini-1.0-pro, gemini-1.5-pro-preview-0409)
AIMS_GOOGLE_LLM_MODEL_ID=gemini-1.0-pro

# --- Celery Configuration ---
# URL for the Celery message broker (e.g., Redis)
CELERY_BROKER_URL=redis://redis:6379/0
# URL for the Celery result backend (e.g., Redis)
CELERY_RESULT_BACKEND=redis://redis:6379/0
