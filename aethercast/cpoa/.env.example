# Service URLs for CPOA to interact with other microservices
PSWA_SERVICE_URL=http://localhost:5004/weave_script
VFA_SERVICE_URL=http://localhost:5005/forge_voice
ASF_NOTIFICATION_URL=http://localhost:5006/asf/internal/notify_new_audio
ASF_WEBSOCKET_BASE_URL=ws://localhost:5006/api/v1/podcasts/stream
SCA_SERVICE_URL=http://localhost:5002/craft_snippet

# Database path for CPOA.
# For snippet generation, this should point to the shared 'topics_snippets' table
# in the database managed by the API Gateway (e.g., ../api_gateway/aethercast_podcasts.db).
# For other CPOA-specific tasks (if any develop), it could be a local DB.
# The default in main.py is "cpoa_orchestration_tasks.db" if this var is not set.
CPOA_DATABASE_PATH=../api_gateway/aethercast_podcasts.db

# Retry mechanism for calls to external services (PSWA, VFA, ASF, SCA)
CPOA_SERVICE_RETRY_COUNT=3
# Backoff factor for exponential backoff (e.g., 0.5 means sleep times: 0.5s, 1s, 2s for retries)
CPOA_SERVICE_RETRY_BACKOFF_FACTOR=0.5

# Placeholder for WCHA if it were a service. Currently, WCHA is a direct import.
# WCHA_SERVICE_URL=http://localhost:5003/harvest_content_endpoint
