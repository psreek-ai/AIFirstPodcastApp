# ASF_SECRET_KEY: Secret key for Flask session management. Change this in production!
# If not set, a temporary UUID will be generated at startup (check logs).
ASF_SECRET_KEY=your_very_secret_key_here_or_generate_one

# ASF_CORS_ALLOWED_ORIGINS: Comma-separated list of allowed origins for SocketIO, or "*" for all.
ASF_CORS_ALLOWED_ORIGINS=*

# ASF_CHUNK_SIZE: Size of audio chunks (in bytes) for streaming.
ASF_CHUNK_SIZE=4096

# ASF_STREAM_SLEEP_INTERVAL: Sleep interval (in seconds) between sending audio chunks.
ASF_STREAM_SLEEP_INTERVAL=0.01

# ASF_UI_UPDATES_NAMESPACE: Socket.IO namespace for UI status updates.
ASF_UI_UPDATES_NAMESPACE=/ui_updates

# SHARED_AUDIO_DIR_CONTAINER: This variable is defined in common.env.
# ASF reads audio files from paths provided in notifications. These paths originate from VFA,
# which uses VFA_SHARED_AUDIO_DIR (set to ${SHARED_AUDIO_DIR_CONTAINER} in Docker).
# So, ASF implicitly uses this shared volume by reading the absolute paths.
# No direct env var needed here for ASF to define the path, but this note is for context.

# Flask app parameters (ASF runs on port 5006 by default in docker-compose.yml)
# ASF_HOST=0.0.0.0
# ASF_PORT=5006
# ASF_DEBUG_MODE=True
# FLASK_DEBUG=True
