# Service host and port for AIMS_TTS
AIMS_TTS_HOST=0.0.0.0
AIMS_TTS_PORT=9000

# Flask debug mode (set to 'false' in production)
FLASK_DEBUG=True

# --- Configuration for Google Cloud Services (TTS & GCS) ---
# Path to the GCP service account key JSON file *inside the Docker container*.
# Typically /app/gcp-credentials.json. Place the actual key file in aethercast/aims_tts_service/ as gcp-credentials.json.
# This key is used for Google Cloud Text-to-Speech and Google Cloud Storage.
# See the main project README's 'GCP Prerequisites and Setup' section for details.
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-credentials.json

# --- Google Cloud Storage Configuration ---
# Name of the Google Cloud Storage bucket for storing generated audio files.
# This is typically set in the common.env file at the project root.
# Override here only if a specific bucket is needed for this service. See main README's GCP setup guide.
GCS_BUCKET_NAME=your-aethercast-gcs-bucket-name
# AIMS_TTS_GCS_AUDIO_PREFIX: Prefix (folder path) within the GCS bucket for AIMS_TTS audio files.
AIMS_TTS_GCS_AUDIO_PREFIX=audio/aims_tts/

# --- Google Cloud TTS Default Parameters ---
# Default voice ID to use if not specified in the request.
# Format depends on the TTS provider (e.g., 'en-US-Wavenet-D' for Google).
AIMS_TTS_DEFAULT_VOICE_ID=en-US-Wavenet-D
# Default language code
AIMS_TTS_DEFAULT_LANGUAGE_CODE=en-US
# Default audio format if not specified (e.g., 'MP3', 'OGG_OPUS', 'LINEAR16').
AIMS_TTS_DEFAULT_AUDIO_ENCODING_STR=MP3
# Default speaking rate (0.25 to 4.0)
AIMS_TTS_DEFAULT_SPEAKING_RATE=1.0
# Default speaking pitch (-20.0 to 20.0)
AIMS_TTS_DEFAULT_PITCH=0.0


# --- Temporary Local Shared Audio Directory ---
# Path for temporary local audio files before GCS upload. Primary storage is GCS.
# Used for intermediate files if local processing is needed before upload.
# Ensure this path is writable by the service if used.
# SHARED_AUDIO_DIR_CONTAINER=/shared_audio/aims_tts_temp # Example, if needed
