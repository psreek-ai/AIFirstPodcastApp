# Service host and port for AIMS_TTS
AIMS_TTS_HOST=0.0.0.0
AIMS_TTS_PORT=9000

# Flask debug mode (set to 'false' in production)
FLASK_DEBUG=True

# --- Configuration for Real TTS Provider (e.g., Google Cloud TTS) ---
# These will be used when actual TTS logic is implemented.
# For Google Cloud TTS: Path to your service account key JSON file.
# Required if using real Google TTS. Mount this file into the container or ensure it's accessible.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/gcp-credentials.json

# Default voice ID to use if not specified in the request.
# Format depends on the TTS provider (e.g., 'en-US-Wavenet-D' for Google).
# AIMS_TTS_DEFAULT_VOICE_ID=AetherVoice-Placeholder

# Default audio format if not specified (e.g., 'mp3', 'ogg_opus', 'wav').
# AIMS_TTS_DEFAULT_AUDIO_FORMAT=mp3

# --- Shared Audio Directory (If AIMS_TTS saves files directly) ---
# Path within the container where generated audio files might be saved.
# This would typically be a mounted volume shared with other services like VFA or ASF.
# This is only needed if AIMS_TTS is responsible for saving and serving files,
# rather than returning audio data directly or a URL from a cloud TTS provider.
# SHARED_AUDIO_DIR_CONTAINER=/shared_audio
